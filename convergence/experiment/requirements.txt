# Python dependencies for the encoding/decoding asymmetry
# probing experiment.
#
# Pinned to specific versions for reproducibility. After
# install, setup.sh generates requirements.lock.txt with the
# exact versions resolved.
#
# IMPORTANT: torch / torchvision / torchaudio are NOT pinned
# here because they require the ROCm-specific PyPI index. They
# are installed by setup.sh from
# https://download.pytorch.org/whl/rocm6.4

# Core ML stack
transformers==4.46.3
datasets==3.2.0
accelerate==1.2.1
tokenizers==0.20.3
safetensors==0.4.5

# Tokenizer backends. Mistral-7B-v0.3 (and several other model
# families) ship SentencePiece-based tokenizers; transformers
# imports protobuf to parse the .model file. Both must be
# installed even though they aren't transformers' direct deps.
sentencepiece==0.2.0
protobuf==5.29.1

# Probing infrastructure
scikit-learn==1.5.2
numpy==2.1.3
scipy==1.14.1

# Data handling
pandas==2.2.3
pyarrow==18.1.0
tabulate==0.9.0  # required by pandas.DataFrame.to_markdown (stage 06)

# Plotting
matplotlib==3.9.3
seaborn==0.13.2

# Config and utilities
pyyaml==6.0.2
tqdm==4.67.1
rich==13.9.4

# HuggingFace Hub for model/dataset version pinning
huggingface-hub==0.26.5
